<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".AdminRequestActivity"
    >

    <include
        android:id="@+id/Admin_List_activity_toolbar"
        layout="@layout/toolbar_layout">
    </include>
    <androidx.appcompat.widget.SearchView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/search_bar_adminId"
        app:iconifiedByDefault="false"
        android:layout_below="@id/Admin_List_activity_toolbar"
        android:layout_margin="5dp"/>
    <ListView
        android:id="@+id/listAdminID"
        android:layout_width="385dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_bar_adminId"
        android:layout_alignParentStart="true"
        android:padding="4dp"
        android:layout_marginTop="25dp"
        android:layout_marginLeft="0dp"
        android:layout_marginRight="0dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:background="@drawable/inputs"
        android:textAlignment="center"
        android:textColor="@color/colorPrimaryDark"
        android:textStyle="bold"
        android:gravity="center_horizontal" />
</RelativeLayout>